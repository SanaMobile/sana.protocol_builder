import { authenticatePage } from '../../../helpers/authenticate-page';
import { module, test } from 'qunit';

module('Unit | Helper | authenticate page');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = authenticatePage(42);
  assert.ok(result);
});
